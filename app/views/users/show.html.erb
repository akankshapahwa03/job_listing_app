<div class="container-fluid">
    <h4><%= image_tag(@user.display_image, style: "border-radius: 50%; width: 90px; height: 90px;") if @user.image.attached? %> <%= @user.name %></h4>
    <h4>  <%= @user.email %></h4>
    <%= link_to "Edit", edit_user_path(@user), class: "btn btn-sm btn-dark" %>
</div>